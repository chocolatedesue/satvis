// Ambient stub for the untyped `cesium-sensor-volumes` GitHub fork.
// Tighten when migrating SatelliteComponentCollection (Phase 2) if desired.
declare module "cesium-sensor-volumes" {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const value: any;
  export default value;
}
