# Sky box faces built by scripts/starmap/generate.sh. Generated, so ignored —
# but the directory is tracked so the generator has a bind mount target and does
# not have to create one (docker would create a missing one owned by root).
*
!.gitignore
